{
  "name": "electron-quick-start-typescript",
  "version": "1.0.0",
  "main": "./dist/main.js",
  "productName ": "ark3R",
  "description": "A minimal Electron application written with Typescript",
  "scripts": {
    "build:render": "cd app && node scripts/build.js",
    "build:app": "tsc",
    "build": "npm run build:render && npm run build:app && copy build.json dist\\package.json ",
    "start": "npm run build && electron ./dist/main.js",
    "pack": "npm run build && npm run pack-only",
    "pack-only": "cd dist && electron-packager . ark3 --platform=win32 --arch=x64 --electron-version=10.1.2",
    "dev:render":"cd app && npm run start",
    "watch": "tsc -w",
    "lint": "tslint -c tslint.json -p tsconfig.json"
  },
  "repository": "https://github.com/electron/electron-quick-start-typescript",
  "keywords": [
    "Electron",
    "quick",
    "start",
    "tutorial",
    "demo",
    "typescript"
  ],
  "author": "GitHub",
  "license": "CC0-1.0",
  "devDependencies": {
    "electron": "^9.0.4",
    "tslint": "^6.1.2",
    "typescript": "^3.9.5"
  }
}